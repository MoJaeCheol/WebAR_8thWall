# 인수인계 — 다음 세션용

저장소: https://github.com/MoJaeCheol/WebAR_8thWall
배포: Render (`*.onrender.com`) — `IMMERSAL_TOKEN`, `IMMERSAL_MAP_IDS` 환경변수 필요

> **2026-08-20 갱신**: 자체 VPS(LiDAR 기반)를 1급 경로로 결정하고 §5 계획의
> P0~P3 를 구현했다. PC 검증(합성 테스트 12개, 서버 스모크) 완료, 실기기 미검증.
> 상세는 §5-1 과 `vps/README.md`, `capture-ios/README.md`.

---

## 1. 무엇을 만들고 있나

로컬/Render 에서 도는 WebAR 프로토타입. 도라에몽 "어디로든 문" 테마.

```
바닥에 문이 고정돼 있고 → 탭하면 열림(포털) → 주변 아이템 수집
        ↑
   VPS 로 실제 공간에 정렬. 누가 언제 와도 같은 자리.
```

**최종 목표는 야외 상용 전시.** 지금은 사무실에서 기술 검증 중.

기술 스택: **8th Wall**(카메라 + SLAM, 오픈소스) + **Immersal**(VPS 측위) + A-Frame.

---

## 2. 현재 상태

### 동작하는 것

- 8th Wall 카메라·SLAM 구동, iOS Safari 에서 실행
- Immersal 측위 성공 (성공률 대략 50~60%)
- 맵 특징점(23,648개) AR 로 겹쳐 보기
- 서버 앵커 저장/조회/삭제 — 개발자가 지정한 위치가 모든 접속자에게 동일 적용
- 개발자 모드(우상단 ⚙) — 진단·배치·기준자
- 자기 진단 — 스케일 비율, 편차, 측위 오차 추정, 변환 일치도

### 안 되는 것 (남은 문제)

| 증상 | 상태 |
|---|---|
| **1m 기준자가 2m 로 보임** | 씬이 미터 단위가 아님. 코칭 오버레이 추가했으나 **효과 미검증** |
| **트래킹 불안정** — 기준자마저 안 붙어 있음 | 8th Wall SLAM 초기화 문제로 추정. 미검증 |
| 측위한 자리 밖으로 가면 어긋남 | 위 두 개의 결과로 보임 |

> ⚠ **마지막 커밋(`dbe07df`)의 코칭 오버레이는 실기기에서 아직 테스트 안 됐다.**
> 다음 세션 첫 작업은 이걸 확인하는 것. 시작할 때 **폰을 앞뒤로 천천히 움직여야** 한다.

---

## 3. 확정된 사실 (다시 조사하지 말 것)

이 프로젝트에서 시간을 가장 많이 쓴 부분들. 근거까지 확인된 것만 적는다.

### 8th Wall

- **App Key 불필요.** 2026-02 호스팅 종료 후 [8thwall.org](https://8thwall.org) 오픈소스 전환.
  엔진은 CDN `@8thwall/engine-binary` 로 무료 로드. 예전 문서의 `appKey=` 방식은 무시.
- 엔진 바이너리 유지보수는 **2026-03 까지**. 이후 무보수, SLAM 은 오픈소스 아님(수정 불가).
- `xrweb` 은 `scale` 을 엔진에 전달한다 (바이너리에서
  `XR8.XrController.configure({disableWorldTracking, scale, ...})` 확인).
- **`scale: absolute` 는 속성만으로 안 잡힌다.** 초기화 때 기기를 앞뒤로 움직여 시차를
  만들어야 한다. 그 동작을 유도하는 `@8thwall/coaching-overlay` 를 반드시 함께 써야 한다.
- `reality.intrinsics` 는 **렌더용 투영행렬**(GraphicsCamera). 화면 종횡비에 맞춰 한 축이
  잘려 있어 **카메라 내부 파라미터로 쓰면 안 된다.** 전체 프레임에 적용하면 크롭 배수만큼
  초점거리가 과대평가된다.

### Immersal

- **좌표 규약 = 이 프로젝트의 규약 2번** (전치 없이 `post = diag(1,-1,-1)`).
  근거: 공식 WebAR 구현 [immersal/vps-for-web](https://github.com/immersal/vps-for-web) 이
  `axisRot.rotateX(Math.PI)` 를 응답 회전에 후곱하고 `r00..r22` 를 행 우선 해석.
  합성 검증에서 규약 2번만 0m/0°, 규약 0번은 **회전 180° 뒤집힘**.
- **초점거리는 계산하는 값이 아니라 조회/추정하는 값.** 공식 구현은 기기 DB(`/devget`)를
  쓰고, 없으면 측위를 누적해 추정한다(16회).
- **맵 좌표는 미터.** (맵 범위 10.9×13.9m ↔ 실제 공간 11×14m 로 확인)
- `/ecef` 응답 13번째 값이 `1.8847` 인데 의미 미확인. 배율이라면 산수가 안 맞는다
  (배율이면 측정 비율이 1.06 이어야 하는데 실측 1.6). **미해결 의문점.**

### Immersal 무료 플랜 한계 (요금표 확인)

| | Free | Pro $99/월 |
|---|---|---|
| **Immersal VPS for Web** | **✗** | ✓ |
| `/devget` 기기 카메라 DB | ✗ (`error: "level"`) | ✓ |
| 맵당 이미지 | 100 | 500 |
| Spatial Map Editing | ✗ | ✓ |
| Map Stitching | ✗ | ✗ (Enterprise 전용) |
| **상업적 사용** | **✗** | ✓ |

`imageCount` 는 현재 **100/100 (꽉 참)**. 새 맵을 만들려면 기존 맵을 지워야 한다.

### 현재 맵

- ID **150559** (`test2`), 100장, 특징점 **23,648개**, 상태 done
- 범위 10.9 × 5.6 × 13.9 m, GPS 37.5096 / 127.0582
- 분포: 중심 반경 **3m 안에 83%**, 5m 안에 92% — 성긴 맵이 아니라 촘촘하다
- 이전 맵 150527(61장, 2,487점)은 삭제됨

### 대안 조사 결과

- **MultiSet AI** — WebXR 1급 지원, 스티칭 포함, LiDAR 임포트, 무료 티어에 상업 사용 포함.
  **그러나 iOS 미지원** (`camera-access` WebXR 기능 필요, Safari 미구현). 안드로이드 전용.
  → 전시용으로는 탈락.
- **ARCore Geospatial** — 1~5m 정확도, 매핑 불필요. 네이티브 전용.
- **Zapworks Mattercraft** — Immersal 1차 통합 + 자체 월드 트래킹. 뷰당 과금.
- 학술 비교에서 **야외 정확도는 Immersal 이 9.4cm 로 1위**로 보고됨(요약 기준, 본문 미확인).

---

## 4. 해결한 버그 (같은 실수 반복 금지)

| 커밋 | 문제 |
|---|---|
| `5b61e97` | 월드 좌표를 `#contentRoot` 로컬 position 에 그대로 대입 → 측위 후 콘텐츠 실종 |
| `1f7062d` | 측위마다 정렬을 통째로 덮어써 콘텐츠가 주기마다 튐 |
| `8649a8f` | 중력 잠금이 회전만 평탄화하고 평행이동을 안 고침 → 세션마다 위치가 달라짐 |
| `f520c98` | 이미지와 카메라 포즈가 다른 프레임 / `pixels` 가 재사용 버퍼라 인코딩 시점엔 다음 프레임 |
| `f520c98` | 진단이 표본 2개·기준선 0.6m 라 값이 요동 → 전체 쌍 최소자승 + 편차 |
| `db0a7c6` | 문서 표현만 보고 좌표 규약을 0번으로 바꿈(오판). 공식 구현 기준 2번이 정답 |
| `b6d4e95` | 프로덕션 정적 캐시 1시간 → 고친 코드가 폰에 안 내려감 |
| `ab730b8` | `Cache-Control` 만으로는 이미 캐시된 응답을 못 되돌림 → BUILD_ID 로 URL 무효화 |

**교훈**: 문서 표현보다 공식 구현 코드가 우선. 그리고 계측 없이 고치면 맞는지 알 수 없다.

---

## 5. 다음 방향 — 자체 VPS (LiDAR 기반)

상용 서비스 없이 직접 구현한다. 비용 $0, 호출·맵 제한 없음.

### 왜

- Immersal 무료는 웹 VPS 미지원 + 상업 사용 불가 → 어차피 유료가 전제
- 자체 구현하면 **좌표 규약·단위·초점거리를 우리가 정하므로** 오늘 겪은 문제군이 사라짐
- 전시는 VPS 의 "쉬운 경우" — 영역 한정, 조명 예측 가능, 재스캔 자유

### 아키텍처

```
[캡처]  iPhone LiDAR → RGB + 깊이(m) + ARKit 포즈(m) + intrinsics
                ↓
[맵 구축]  PC/Python — 특징점 추출 → 깊이로 역투영 → (서술자, 3D 미터 좌표)
                ↓                      ※ SfM/COLMAP 불필요, 스케일 모호성 없음
[측위 서버]  PC/FastAPI — 이미지 수신 → 매칭 → solvePnPRansac → 포즈 반환
                ↓
[클라이언트]  8th Wall SLAM + 우리 측위 서버. 기존 코드에서 엔드포인트만 교체
```

**핵심**: LiDAR 깊이가 있으면 삼각측량 없이 3D 좌표가 나오고 **미터가 보장**된다.

### 맵 빌더는 입력 두 가지를 다 받게 설계할 것

| 입력 | 3D 복원 방법 | 특징 |
|---|---|---|
| **A. LiDAR 프레임 + ARKit 포즈** | 깊이로 역투영 | 정확, 미터 보장. 네이티브 캡처 필요 |
| **B. 웹 프레임 + 8th Wall 포즈** | 포즈 기지 삼각측량 | 캡처 코드가 이미 90% 있음. 스케일이 런타임과 자동 상쇄 |

산출물 포맷을 같게 하면 다운스트림(측위 서버·클라이언트)은 공통이다.

### 단계별 계획

| # | 작업 | 산출물 | 비고 |
|---|---|---|---|
| 1 | **캡처** | RGB + 깊이 + 포즈 + intrinsics 파일 | Mac 되면 직접 앱, 아니면 Stray Scanner 등 |
| 2 | **맵 빌더** (Python + OpenCV) | `(서술자, 3D점)` 맵 파일 | SIFT 로 시작. GPU 불필요 |
| 3 | **측위 서버** (FastAPI) | `POST /localize` → 포즈 | `solvePnPRansac`. CPU 충분 |
| 4 | **클라이언트 전환** | 엔드포인트 교체 | `immersal.js` 를 인터페이스로 추상화 |
| 5 | **A/B 검증** | Immersal vs 자체 비교 | 기존 진단 도구 그대로 사용 |

### 이 방식으로 사라지는 문제

| 오늘의 문제 | 자체 구현 시 |
|---|---|
| 좌표 규약을 모름 | **내가 정함** |
| 맵이 미터인지 모름 | **ARKit 이 보장** |
| 초점거리를 못 얻음 | 캡처 데이터에 들어있음. 또는 맵 구축 시 재투영 오차 최소화로 추정 |
| 호출·맵 제한 | 없음 |
| 벤더 리스크 | 없음 |

### 남는 문제

**8th Wall 씬 스케일**은 그대로다. LiDAR 맵은 미터인데 런타임 포즈는 8th Wall 기준이라
스케일을 이어줘야 한다. → 이미 만들어둔 `sceneScale`(닮음변환) + 1m 기준자로 처리.

### 확인 필요

- [x] 기기: **iPhone Pro (LiDAR 있음)**, 캡처는 **자작 앱** (Mac+Xcode) — 사용자 확정
- [ ] Mac 에서 캡처 앱 첫 빌드 (절차: `capture-ios/README.md`)

## 5-1. 구현 현황 (2026-08-20 — 이 세션에서 구현)

**LiDAR 를 1급 경로로 승격한 근거** (비교 분석 후 사용자 결정):
맵이 진짜 미터(±1~5cm) / ARKit 포즈라 8th Wall 트래킹 불안정 문제를 우회 /
공장 캘리브레이션 intrinsics 라 초점거리 추정 불필요 / 오클루전·실측 저작 가능.
LiDAR 유효거리(~5m) 밖 특징점은 포즈 기지 삼각측량으로 폴백(하이브리드 빌더).
단 **런타임(관람객 폰)은 여전히 8th Wall** — LiDAR 는 맵 제작 전용이다.

### 완료 (PC 검증)

| 단계 | 내용 | 검증 |
|---|---|---|
| P0 기하·규약 | `vps/common/geometry.py` 가 GL↔CV 변환 단일 소유. 응답은 GL camera-to-map (Immersal 필드명) → 클라이언트 규약 0번 | 합성 왕복 테스트 |
| P1 맵 빌더 | SIFT+rootSIFT → 깊이 역투영(1급)/삼각측량(폴백) → 재투영 검증 → npz+PLY | 합성 E2E |
| P1 측위 서버 | FastAPI :8000, FLANN+solvePnPRansac, 수락 게이트 inliers≥20 | 합성 측위 오차 ~0 |
| P2 캡처 앱 | `capture-ios/` Swift 소스 + 빌드 README (Xcode 프로젝트는 Mac 에서 생성) | 미빌드 |
| P2 가져오기 | `import_dataset.py` — zip 검증 → data/datasets/ | 코드만 |
| P3 통합 | Node `/api/vps/*` 프록시, 클라이언트 백엔드 토글(개발자 패널), sceneScale 즉시적용 버그 수정, 측위 재시작 버튼 | 스모크 통과 |

`python -m pytest vps/tests` **12개 전부 통과** (기하 왕복 / JSON 규약 왕복 /
역투영·삼각측량 정확도 / fx 그리드 서치 / 맵 IO·PLY / 측위 / 디스크 E2E).

### 테스트가 잡아낸 함정 (재발 주의)

- **다중 서술자 ratio test**: 맵이 포인트당 서술자 ≤4개를 보관하므로 knn=2 의
  1·2등이 같은 점의 관측들이 되어 ratio≈1 → 참 대응 전멸. 2등을 "다른 3D점"
  기준으로 잡아야 한다 (`localizer._match_map` 주석 참조).
- **입력 B 에피폴라 필터**: fx 가정 오차가 에피폴라 거리를 부풀리므로
  assumed intrinsics 는 12px, device-reported 는 3px 임계값.

### 남은 것 (P4 — 실기기)

1. ~~Mac 에서 `capture-ios` 빌드 → 사무실 캡처~~ **완료 (2026-08-21)** —
   `cap_20260821_100436`, 276프레임, 깊이 커버리지 64%, 동선 27m
2. ~~`import_dataset` → `build_map` → 육안 검사~~ **완료** — 맵 ID **1000001**,
   포인트 26,499개(깊이 18,203/삼각측량 8,296), 빌드 38초.
   3면도(`preview_map.py`)에서 벽·책상 라인 뚜렷, 드리프트 번짐 없음
3. **셀프 측위 검증 완료** — 데이터셋 프레임 8개 재측위:
   **중앙값 위치오차 0.3cm / 회전 0.19°**, 7/8 성공, 질의당 ~150ms.
   (1개 실패는 저텍스처 방향으로 추정 — 실환경 성공률은 폰 테스트로 확인)
4. 폰 웹앱 → 개발자 모드 → **측위 백엔드: 자체 VPS** → 기존 진단으로 판정
   (맵이 미터이므로 스케일 비율 = 씬 스케일 측정값 그대로)
5. 같은 동선으로 Immersal A/B (`진단 리포트 복사` 비교)
6. 병행: 코칭 오버레이/1m 기준자 확인 (§2 의 미해결 문제 — 런타임 쪽은 그대로다)
7. 1280 캡처 해상도에서 FPS 확인 — 드랍이면 config.js `maxDimension: 960`

---

## 6. 코드 지도

```
server.js                    HTTP/HTTPS 서버, Immersal/자체VPS 프록시, 앵커 저장소,
                             포인트클라우드 중계, BUILD_ID 캐시 무효화
public/
  index.html                 씬 정의. 스크립트 로드 순서 주의(a-scene 파싱 전 컴포넌트 등록)
  js/
    config.js                ⚙ 튜닝값 전부 여기 (vps.backend 포함)
    immersal.js              측위 클라이언트 — BACKENDS(immersal/selfvps) 토글,
                             규약/초점거리/진단/닮음변환 정렬
    components/experience.js 개발자·관람 모드, 배치, 앵커, 진단 UI, 백엔드 토글
    ply.js                   맵 포인트클라우드 파서 (자체 맵 PLY 도 동일 형식)
    png.js                   8-bit 그레이스케일 PNG 인코더 (측위 요청 이미지)
vps/                         자체 VPS — 맵 빌더 + 측위 서버 (Python, vps/README.md)
  common/geometry.py         ★ 좌표 규약 단일 소유 (GL↔CV) — 여기만 만진다
  builder/                   import_dataset / build_map / features / matching / …
  server/                    FastAPI :8000 (localizer + main)
  tests/                     합성 E2E — python -m pytest vps/tests
capture-ios/                 LiDAR 캡처 앱 (Swift 소스, Mac 에서 빌드 — README 참조)
data/anchors.json            맵 ID별 문 배치 좌표 (Render 는 휘발성 — 커밋해야 영구)
data/datasets/, data/maps/   캡처·빌드 산출물 (.gitignore — 용량)
```

`README.md` 에 각 결정의 근거가 상세히 적혀 있다. 특히:
- 좌표 규약 2번인 이유
- 초점거리를 렌더 행렬에서 뽑으면 안 되는 이유
- 진단 지표(스케일/편차) 읽는 법
- 코칭 오버레이가 필요한 이유

---

## 7. 개발자 모드 사용법

우상단 **⚙** → localStorage 에 저장돼 새로고침해도 유지.

| 도구 | 용도 |
|---|---|
| 맵 특징점 겹쳐 보기 | 점이 실제 공간과 맞으면 맵·정렬 모두 정상 |
| **1m 기준자** | 씬이 미터인지 직접 확인. VPS 와 무관 |
| 배율 ±10% | 기준자가 실제 1m 로 보일 때까지 손으로 맞춤 |
| 자동보정 켬/끔 | 초점거리 자가 보정 (기본 꺼짐 — 씬 배율과 충돌) |
| 진단 리포트 복사 | 7줄 요약을 클립보드로 |
| 규약 수동 변경 | 0~7 순환 (기본 2번 고정) |

**진단 읽는 법**

| | 뜻 |
|---|---|
| 스케일 ≈ 1.00, 편차 작음 | 정상 |
| 스케일 ≠ 1, **편차 작음** | 계통 오차 — 코드/스케일 문제 |
| **편차 큼** (≥15%) | 측위 불안정 — 맵 품질 문제 |
| 측위오차 ≈±N m | 편차 × 기준선. 맵 정확도의 실질 지표 |

---

## 8. 운영

```bash
npm install
npm start          # HTTP :3000 (PC), HTTPS :3443 (폰, LAN IP 안내 출력)
```

자체 VPS 를 쓸 때는 Python 측위 서버를 별도 창에서 함께 띄운다:

```bash
.venv\Scripts\python -m uvicorn vps.server.main:app --host 127.0.0.1 --port 8000
```

(최초 1회: `python -m venv .venv` 후 `.venv\Scripts\pip install -r vps\requirements.txt`)
Node 가 10초마다 :8000 헬스를 프로브해 `/api/config` 의 `vps.configured` 로 알려준다.

Render 환경변수:
```
IMMERSAL_TOKEN     = (개발자 포털에서 복사)
IMMERSAL_MAP_IDS   = 150559          # 쉼표로 최대 8개
NODE_ENV           = production
```

> ⚠ 토큰이 이전 세션 대화에 평문으로 남아 있다. 개발 끝나면 포털에서 재발급 권장.
> `.env` 는 gitignore 되어 있고 커밋 히스토리에도 없다(확인함).

캐시 문제 시: 주소에 쿼리를 아무거나 하나 더 붙여 열면 된다 (`?x=5`).
화면의 `build` 값이 서버와 다르면 캐시된 옛 파일을 보고 있는 것.

---

## 9. 다음 세션 첫 작업

1. **Mac 에서 `capture-ios` 빌드** (`capture-ios/README.md` — 5분 절차) → 사무실 캡처
2. 캡처 zip → `import_dataset` → `build_map` → **PLY 육안 검사** (§5-1 의 P4 절차)
3. 두 서버 기동 → 폰에서 **자체 VPS 백엔드** 로 측위 → 기존 진단으로 판정 → Immersal A/B
4. 병행: **코칭 오버레이/1m 기준자 확인** — 런타임 씬 스케일 문제는 여전히 미검증이다.
   맵이 미터가 됐으므로 스케일 비율 판독이 더 직접적이다 (비율 = 씬 배율 그 자체)
