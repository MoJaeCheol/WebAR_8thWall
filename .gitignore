node_modules/
certs/
.env
.DS_Store

# Python (자체 VPS)
.venv/
__pycache__/
*.pyc
.pytest_cache/

# 캡처 데이터셋 (수백 MB — git 에 안 올림. 맵(data/maps)은 배포에 필요해서 커밋한다)
data/datasets/

# Xcode (capture-ios 프로젝트는 Mac 에서 생성)
capture-ios/*.xcodeproj
xcuserdata/
*.zip
